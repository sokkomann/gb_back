<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.bideo.mapper.common.SearchHistoryMapper">

    <select id="selectRecentSearches" resultType="SearchHistoryResponseDTO">
        SELECT id, keyword, searched_at
        FROM tbl_search_history
        WHERE member_id = #{memberId}
        ORDER BY searched_at DESC
        LIMIT 10
    </select>

    <insert id="insertSearchHistory">
        INSERT INTO tbl_search_history (member_id, keyword)
        VALUES (#{memberId}, #{keyword})
    </insert>

    <delete id="deleteSearchHistory">
        DELETE FROM tbl_search_history
        WHERE id = #{id}
          AND member_id = #{memberId}
    </delete>

    <select id="selectTrendingKeywords" resultType="TrendingKeywordDTO">
        SELECT keyword, COUNT(*) AS search_count
        FROM tbl_search_history
        WHERE searched_at >= NOW() - INTERVAL '7 days'
        GROUP BY keyword
        ORDER BY search_count DESC
        LIMIT 8
    </select>

</mapper>
