<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.bideo.mapper.auction.BidMapper">

    <insert id="insertBid" parameterType="BidVO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tbl_bid (auction_id, member_id, bid_price, is_winning)
        VALUES (#{auctionId}, #{memberId}, #{bidPrice}, #{isWinning})
    </insert>

    <select id="selectByAuctionId" resultType="BidResponseDTO">
        SELECT
            b.id,
            b.auction_id,
            b.member_id,
            m.nickname AS member_nickname,
            b.bid_price,
            b.is_winning,
            b.created_datetime
        FROM tbl_bid b
        JOIN tbl_member m ON b.member_id = m.id
        WHERE b.auction_id = #{auctionId}
        ORDER BY b.bid_price DESC, b.created_datetime DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="updatePreviousWinning">
        UPDATE tbl_bid
        SET is_winning = false
        WHERE auction_id = #{auctionId}
          AND is_winning = true
    </update>

    <select id="selectHighestBid" resultType="BidResponseDTO">
        SELECT
            b.id,
            b.auction_id,
            b.member_id,
            m.nickname AS member_nickname,
            b.bid_price,
            b.is_winning,
            b.created_datetime
        FROM tbl_bid b
        JOIN tbl_member m ON b.member_id = m.id
        WHERE b.auction_id = #{auctionId}
        ORDER BY b.bid_price DESC
        LIMIT 1
    </select>

    <select id="selectBidderIds" resultType="long">
        SELECT DISTINCT member_id
        FROM tbl_bid
        WHERE auction_id = #{auctionId}
    </select>
</mapper>
